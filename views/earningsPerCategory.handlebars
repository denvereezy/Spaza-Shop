<div id="container"><h2>Earnings Per Category</h2>

<table class="table table-striped custab">
    <thead>
        <tr>
            <th>Product</th>
            <th>Amount(R)</th>
        </tr>
    </thead>
            <tr>
                {{#each products}}
                <td>{{Name}}</td>
                <td>{{TotalQty}}</td>
            </tr>
            {{/each}}
    </table></div>
